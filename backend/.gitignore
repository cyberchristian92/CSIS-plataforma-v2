node_modules
/dist
# Keep environment variables out of version control
.env

/generated/prisma

# Arquivos enviados pelos usuários (perícia) — não versionar
/uploads

# Diretório de trabalho da compilação de laudos (markdown + PDF gerado) — não versionar
/laudo-workdir
